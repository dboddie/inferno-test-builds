name: test-builds
run-name: Build Inferno
on: [push]
jobs:
  clone-and-build-repo:
    runs-on: ubuntu-latest
    steps:
      - run: sudo dpkg --add-architecture i386
      - run: git clone -b stm32f405 https://github.com/dboddie/inferno-os.git
